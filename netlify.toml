# Netlify configuration for ExPorta GST (React + Vite SPA)

[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application (SPA) routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
